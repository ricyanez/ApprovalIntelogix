@model ApprovalIntelogix.Models.UserStep

@{
    ViewData["Title"] = "Edit";
}

<h1>Edit</h1>

<h4>UserStep</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="IdUserStep" />
            <div class="form-group">
                <label asp-for="PkId" class="control-label"></label>
                <select asp-for="PkId" class="form-control" asp-items="ViewBag.PkId"></select>
                <span asp-validation-for="PkId" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="IdStep" class="control-label"></label>
                <select asp-for="IdStep" class="form-control" asp-items="ViewBag.IdStep"></select>
                <span asp-validation-for="IdStep" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
